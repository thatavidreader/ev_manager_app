name: ev_manager_app
description: "A modern EV Manager app with an electric vehicle theme."

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  firebase_auth: ^5.5.2
  firebase_core: ^3.13.0
  cloud_firestore: ^5.6.6
  http: ^1.3.0
  provider: ^6.0.0
  percent_indicator: ^4.2.2
  animated_text_kit: ^4.2.2
  flutter_map: ^8.1.1
  latlong2: ^0.9.0
  geolocator: ^13.0.4
  permission_handler: ^11.0.1


flutter:
  uses-material-design: true
